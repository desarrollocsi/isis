import { StatusDinamicoPipe } from './status-dinamico.pipe';

describe('StatusDinamicoPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusDinamicoPipe();
    expect(pipe).toBeTruthy();
  });
});
